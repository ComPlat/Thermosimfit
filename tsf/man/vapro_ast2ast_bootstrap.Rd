% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OptimizeVapro_ast2ast.R
\name{vapro_ast2ast_bootstrap}
\alias{vapro_ast2ast_bootstrap}
\title{Parametric bootstrap for a VAPRO fit, using the ast2ast-compiled grid
search for every fit (point estimate + all nBoot replicates), for any of
the four cases.}
\usage{
vapro_ast2ast_bootstrap(
  case,
  lowerBounds,
  upperBounds,
  path,
  additionalParameters,
  nBoot = 500L,
  sigma = NULL,
  nGrid = 1000L,
  seed = NULL,
  showProgress = TRUE
)
}
\arguments{
\item{case}{one of "dba_dye_const", "dba_host_const", "ida" or "gda".}

\item{lowerBounds, upperBounds}{bounds for the nonlinear binding parameter.}

\item{path}{file path or already-loaded data.frame, same as opti_vapro.}

\item{additionalParameters}{same as opti_vapro.}

\item{nBoot}{number of bootstrap replicates (default 500).}

\item{sigma}{per-signal noise sd; NULL (default) estimates it from the
residuals of the original fit. A single value is recycled.}

\item{nGrid}{number of exponential grid points, for the original fit and
every replicate (default 1000).}

\item{seed}{RNG seed; if NULL the current time is used.}

\item{showProgress}{print a text progress bar (default TRUE).}
}
\value{
a list: draws (one row per successful replicate), summary
(mean/sd/2.5\%/50\%/97.5\% per parameter), sigma, n_success, n_fail,
seed, point_estimate (the original GridResult).
}
\description{
Same design as opti_vapro_bootstrap(): fit once, estimate per-signal
noise sigma from the residuals of that fit (unless sigma is supplied),
then for each replicate simulate new Gaussian noise around the fitted
curve and refit. Noise generation stays in R -- ast2ast has no RNG, and
reproducibility (set.seed()) depends on R's own RNG algorithm anyway, so
there'd be nothing to gain compiling that part. Every \emph{fit} (the coarse
grid search over the nonlinear binding parameter, root-find + NNLS per
grid point) runs compiled.
}
